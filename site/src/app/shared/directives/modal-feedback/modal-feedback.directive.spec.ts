import { ModalFeedbackDirective } from './modal-feedback.directive';

describe('ModalFeedbackDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalFeedbackDirective();
    expect(directive).toBeTruthy();
  });
});
